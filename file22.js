// intro to for loop 
//  print 0 to 9 

for ( let i = 0 ; i <= 9 ; i++){
    console.log (i);
}
// console.log (i);
//  if we will use let the variable will not exist outside the scope ...
// this would return an error 
// But if we use var this will be existing outside the scope too ...
